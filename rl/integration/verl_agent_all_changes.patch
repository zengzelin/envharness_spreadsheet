diff --git a/agent_system/environments/env_manager.py b/agent_system/environments/env_manager.py
index ab09dff..1123b29 100644
--- a/agent_system/environments/env_manager.py
+++ b/agent_system/environments/env_manager.py
@@ -609,6 +609,66 @@ def make_envs(config):
     group_n = config.env.rollout.n if config.env.rollout.n > 0 else 1
     resources_per_worker = OmegaConf.to_container(config.env.resources_per_worker, resolve=True)
 
+    if config.env.env_name.lower().startswith("envharness_rl/spreadsheetbench"):
+        import sys as _sys
+        from pathlib import Path as _Path
+
+        # This checkout lives at <envharness>/third_party/verl-agent. Resolve
+        # both tracked packages relative to it so host and cluster mount paths
+        # work without hardcoded prefixes.
+        _envharness_root = _Path(__file__).resolve().parents[4]
+        for _path in (_envharness_root, _envharness_root / "rl"):
+            if str(_path) not in _sys.path:
+                _sys.path.insert(0, str(_path))
+
+        from envharness_rl.spreadsheetbench.envs import (
+            build_envharness_spreadsheetbench_envs,
+        )
+        from envharness_rl.spreadsheetbench.manager import (
+            SpreadsheetBenchEnvironmentManager,
+        )
+        from envharness_rl.spreadsheetbench.projection import (
+            envharness_spreadsheetbench_projection,
+        )
+
+        _data_path = os.environ.get("SPREADSHEETBENCH_DATA", "")
+        if not _data_path:
+            raise RuntimeError(
+                "envharness_rl/spreadsheetbench requires "
+                "$SPREADSHEETBENCH_DATA"
+            )
+        _reset_options = {
+            "data_path": _data_path,
+            "max_steps": config.env.max_steps,
+            "step_timeout": int(os.environ.get(
+                "SPREADSHEETBENCH_STEP_TIMEOUT", "60")),
+            "obs_truncate": int(os.environ.get(
+                "SPREADSHEETBENCH_OBS_TRUNCATE", "6000")),
+        }
+        _envs = build_envharness_spreadsheetbench_envs(
+            seed=config.env.seed,
+            env_num=config.data.train_batch_size,
+            group_n=group_n,
+            resources_per_worker=resources_per_worker,
+            is_train=True,
+            env_kwargs=_reset_options,
+        )
+        _val_envs = build_envharness_spreadsheetbench_envs(
+            seed=config.env.seed + 1000,
+            env_num=config.data.val_batch_size,
+            group_n=1,
+            resources_per_worker=resources_per_worker,
+            is_train=False,
+            env_kwargs=_reset_options,
+        )
+        envs = SpreadsheetBenchEnvironmentManager(
+            _envs, envharness_spreadsheetbench_projection, config,
+            split="train")
+        val_envs = SpreadsheetBenchEnvironmentManager(
+            _val_envs, envharness_spreadsheetbench_projection, config,
+            split="val")
+        return envs, val_envs
+
     if "search" in config.env.env_name.lower():
         from agent_system.environments.env_package.search import build_search_envs, search_projection
         _envs = build_search_envs(seed=config.env.seed, env_num=config.data.train_batch_size, group_n=group_n, is_train=True, env_config=config.env)
@@ -628,6 +688,112 @@ def make_envs(config):
         val_envs = GymCardEnvironmentManager(_val_envs, projection_f, config)
         return envs, val_envs
     elif "alfworld" in config.env.env_name.lower():
+        # Route to the release_version_rl adapter if env_name starts with
+        # `envharness_rl/alfworld`. This is ADDITIVE: the legacy
+        # `envharness/alfworld` route below is untouched (active runs depend
+        # on it). The new route builds envs from release_version's
+        # ActionableEnv + Rules harness instead of the old AlfworldBridge +
+        # MutationLayer; both `release_version/` and `release_version_rl/`
+        # must be on PYTHONPATH (the launcher script adds them).
+        if config.env.env_name.lower().startswith("envharness_rl/alfworld"):
+            import sys as _sys
+            from pathlib import Path as _Path
+            _repo_root = _Path(__file__).resolve().parents[3]
+            for _p in (_repo_root / "release_version",
+                       _repo_root / "release_version_rl"):
+                if str(_p) not in _sys.path:
+                    _sys.path.insert(0, str(_p))
+            from envharness_rl.alfworld import (
+                build_envharness_alfworld_envs, envharness_alfworld_projection,
+            )
+            alf_config_path = os.path.join(os.path.dirname(__file__),
+                'env_package/alfworld/configs/config_tw.yaml')
+            env_kwargs = {
+                'eval_dataset': config.env.alfworld.eval_dataset,
+                # Restrict TRAIN to a small game_file subset (one JSONL line
+                # per {"game_file": ...}). Gated on is_train inside
+                # EnvharnessAlfworldEnvs so eval splits keep the full set.
+                'train_subset_path': os.environ.get('ALFWORLD_TRAIN_SUBSET_PATH'),
+                # Per-game Rules corpus (rules_code + in_env_actions) built by
+                # release_version_rl/scripts/build_corpus.py. Train-only; eval
+                # workers always use the vanilla env so SR stays comparable to
+                # the unmutated baseline.
+                'mutation_corpus_path': os.environ.get('ENVHARNESS_MUTATION_CORPUS'),
+                'subset_authoritative': os.environ.get(
+                    'ENVHARNESS_SUBSET_AUTHORITATIVE', '').lower()
+                    in ('1', 'true', 'yes'),
+            }
+            _envs = build_envharness_alfworld_envs(
+                alf_config_path, config.env.seed,
+                config.data.train_batch_size, group_n,
+                is_train=True, env_kwargs=env_kwargs,
+                resources_per_worker=resources_per_worker)
+            _val_envs = build_envharness_alfworld_envs(
+                alf_config_path, config.env.seed + 1000,
+                config.data.val_batch_size, 1,
+                is_train=False, env_kwargs=env_kwargs,
+                resources_per_worker=resources_per_worker)
+            projection_f = partial(envharness_alfworld_projection)
+            envs = AlfWorldEnvironmentManager(_envs, projection_f, config)
+            val_envs = AlfWorldEnvironmentManager(_val_envs, projection_f, config)
+            return envs, val_envs
+
+        # Route to EnvHarness-backed adapter if env_name starts with `envharness/`.
+        # Otherwise fall through to verl-agent's stock alfworld path.
+        if config.env.env_name.lower().startswith("envharness/alfworld"):
+            # Lazy-import so plain verl-agent runs don't require EnvHarness on PATH.
+            import sys as _sys
+            from pathlib import Path as _Path
+            _envharness_root = _Path(__file__).resolve().parents[3]
+            if str(_envharness_root) not in _sys.path:
+                _sys.path.insert(0, str(_envharness_root))
+            from envharness.rl.envharness_alfworld import (
+                build_envharness_alfworld_envs, envharness_alfworld_projection,
+            )
+            alf_config_path = os.path.join(os.path.dirname(__file__),
+                'env_package/alfworld/configs/config_tw.yaml')
+            env_kwargs = {
+                'eval_dataset': config.env.alfworld.eval_dataset,
+                # SkillOS-inspired: shrink TRAIN to a type-balanced subset.
+                # Set ALFWORLD_TRAIN_SUBSET_PATH env var to a JSONL with
+                # 'game_file' keys (see examples/build_alfworld_train_subset.py).
+                # When unset, full ~3553-task TRAIN is used. This same
+                # env_kwargs dict is handed to BOTH the train and val envs
+                # below, but EnvharnessAlfworldEnvs gates train_subset_path on
+                # is_train (see envs.py) so it is dropped for eval splits --
+                # the subset JSONL lists TRAIN game_files, which match zero
+                # eval games (the bridge would raise on a zero-match filter).
+                'train_subset_path': os.environ.get('ALFWORLD_TRAIN_SUBSET_PATH'),
+                # Optional: per-game-file mutation corpus produced by
+                # examples/build_mutation_corpus.py. When set, train workers
+                # apply the matching MutationLayer per episode (Setting A:
+                # base-Qwen3-targeted; Setting B phase 2: step_30-targeted).
+                # Gated on is_train inside EnvharnessAlfworldEnvs so eval
+                # workers always use the vanilla env -- SR stays comparable
+                # to the unmutated step_60 baseline.
+                'mutation_corpus_path': os.environ.get('ENVHARNESS_MUTATION_CORPUS'),
+                # AUGMENT/COMBINE: when set, the train subset is authoritative
+                # so mutated game COPIES outside alfworld's scanned dir load.
+                # Gated on is_train inside EnvharnessAlfworldEnvs.
+                'subset_authoritative': os.environ.get(
+                    'ENVHARNESS_SUBSET_AUTHORITATIVE', '').lower()
+                    in ('1', 'true', 'yes'),
+            }
+            _envs = build_envharness_alfworld_envs(
+                alf_config_path, config.env.seed,
+                config.data.train_batch_size, group_n,
+                is_train=True, env_kwargs=env_kwargs,
+                resources_per_worker=resources_per_worker)
+            _val_envs = build_envharness_alfworld_envs(
+                alf_config_path, config.env.seed + 1000,
+                config.data.val_batch_size, 1,
+                is_train=False, env_kwargs=env_kwargs,
+                resources_per_worker=resources_per_worker)
+            projection_f = partial(envharness_alfworld_projection)
+            envs = AlfWorldEnvironmentManager(_envs, projection_f, config)
+            val_envs = AlfWorldEnvironmentManager(_val_envs, projection_f, config)
+            return envs, val_envs
+
         from agent_system.environments.env_package.alfworld import build_alfworld_envs, alfworld_projection
         if config.env.env_name == 'alfworld/AlfredThorEnv':
             alf_config_path = os.path.join(os.path.dirname(__file__), 'env_package/alfworld/configs/config_tw.yaml')
@@ -641,7 +805,7 @@ def make_envs(config):
         }
         _envs = build_alfworld_envs(alf_config_path, config.env.seed, config.data.train_batch_size, group_n, is_train=True, env_kwargs=env_kwargs, resources_per_worker=resources_per_worker)
         _val_envs = build_alfworld_envs(alf_config_path, config.env.seed + 1000, config.data.val_batch_size, 1, is_train=False, env_kwargs=env_kwargs, resources_per_worker=resources_per_worker)
-        
+
         projection_f = partial(alfworld_projection)
         envs = AlfWorldEnvironmentManager(_envs, projection_f, config)
         val_envs = AlfWorldEnvironmentManager(_val_envs, projection_f, config)
@@ -661,6 +825,60 @@ def make_envs(config):
         envs = SokobanEnvironmentManager(_envs, projection_f, config)
         val_envs = SokobanEnvironmentManager(_val_envs, projection_f, config)
         return envs, val_envs
+    elif config.env.env_name.lower().startswith("envharness/webshop") or \
+            "envharness_webshop" in config.env.env_name.lower():
+        # EnvHarness <-> verl-agent WebShop integration. Drives WebShop through
+        # envharness.bridges.webshop.WebshopBridge (one WebAgentTextEnv per Ray
+        # worker) with an optional per-session MutationLayer. Reuses the native
+        # WebshopEnvironmentManager (obs formatting / success) and the native
+        # webshop_projection (raw "search[...]"/"click[...]" parsing).
+        import sys as _sys
+        from pathlib import Path as _Path
+        # repo root = .../EnvHarness (envharness/ lives here). __file__ is
+        # .../EnvHarness/third_party/verl-agent/agent_system/environments/
+        # env_manager.py, so parents[4] is the repo root. (Ray workers also
+        # need it -- the launcher exports PYTHONPATH; this is the in-driver
+        # belt-and-suspenders.)
+        _envharness_root = _Path(__file__).resolve().parents[4]
+        if str(_envharness_root) not in _sys.path:
+            _sys.path.insert(0, str(_envharness_root))
+        from envharness.rl.envharness_webshop import build_envharness_webshop_envs
+        from agent_system.environments.env_package.webshop import webshop_projection
+        _wroot = os.path.join(os.path.dirname(__file__),
+                              'env_package/webshop/webshop')
+        if config.env.webshop.use_small:
+            file_path = os.path.join(_wroot, 'data/items_shuffle_1000.json')
+            attr_path = os.path.join(_wroot, 'data/items_ins_v2_1000.json')
+        else:
+            file_path = os.path.join(_wroot, 'data/items_shuffle.json')
+            attr_path = os.path.join(_wroot, 'data/items_ins_v2.json')
+        env_kwargs = {
+            'observation_mode': 'text',
+            'num_products': None,
+            'human_goals': config.env.webshop.human_goals,
+            'file_path': file_path,
+            'attr_path': attr_path,
+        }
+        # TRAIN-ONLY per-session mutation corpus (eval runs vanilla so SR stays
+        # comparable). Set ENVHARNESS_WEBSHOP_MUTATION_CORPUS to a JSONL of
+        # {"session", "mutation_code", "in_env_actions"} records.
+        _mut = os.environ.get('ENVHARNESS_WEBSHOP_MUTATION_CORPUS')
+        _envs = build_envharness_webshop_envs(
+            seed=config.env.seed, env_num=config.data.train_batch_size,
+            group_n=group_n, is_train=True, env_kwargs=env_kwargs,
+            webshop_root=_wroot, mutation_corpus_path=_mut,
+            resources_per_worker=resources_per_worker)
+        _val_envs = build_envharness_webshop_envs(
+            seed=config.env.seed + 1000, env_num=config.data.val_batch_size,
+            group_n=1, is_train=False, env_kwargs=env_kwargs,
+            webshop_root=_wroot, mutation_corpus_path=None,
+            resources_per_worker=resources_per_worker)
+        projection_f = partial(webshop_projection)
+        envs = WebshopEnvironmentManager(_envs, projection_f, config)
+        val_envs = WebshopEnvironmentManager(_val_envs, projection_f, config)
+        import time
+        time.sleep((config.data.train_batch_size * group_n + config.data.val_batch_size) * 0.1)
+        return envs, val_envs
     elif "webshop" in config.env.env_name.lower():
         from agent_system.environments.env_package.webshop import build_webshop_envs, webshop_projection
         if config.env.webshop.use_small:
@@ -689,11 +907,25 @@ def make_envs(config):
         from agent_system.environments.env_package.appworld import build_appworld_envs, appworld_projection
         _envs = build_appworld_envs(dataset_name='train', seed=config.env.seed, env_num=config.data.train_batch_size, group_n=group_n, start_server_id=0, resources_per_worker=resources_per_worker)
         _val_envs = build_appworld_envs(dataset_name='test_normal', seed=config.env.seed + 1000, env_num=config.data.val_batch_size, group_n=1, start_server_id=config.data.train_batch_size*group_n, resources_per_worker=resources_per_worker)
-        
+
         projection_f = partial(appworld_projection)
         envs = AppWorldEnvironmentManager(_envs, projection_f, config)
         val_envs = AppWorldEnvironmentManager(_val_envs, projection_f, config)
         return envs, val_envs
+    elif "envharness_swegym_openhands" in config.env.env_name.lower():
+        # OpenHands in-context FC variant: same SWE-Gym Bridge + Ray workers,
+        # but the rollout policy acts in `<function=...>` format (for the
+        # NovaSky SWE-Gym-OpenHands / SkyRL checkpoints). MUST be checked
+        # before the plain `envharness_swegym` branch (substring match).
+        from envharness.rl.env_manager import make_envharness_swegym_openhands_envs
+        return make_envharness_swegym_openhands_envs(config)
+    elif "envharness_swegym" in config.env.env_name.lower():
+        # EnvHarness <-> verl-agent integration. Drives SWE-Gym instances
+        # through envharness.bridges.swebench.SWEBenchBridge (Docker per
+        # task) with an optional MutationLayer per env slot. See
+        # envharness/rl/env_manager.py for the Manager class.
+        from envharness.rl.env_manager import make_envharness_swegym_envs
+        return make_envharness_swegym_envs(config)
     else:
         print("Environment not supported")
         exit(1)
\ No newline at end of file
diff --git a/agent_system/environments/env_package/webshop/projection.py b/agent_system/environments/env_package/webshop/projection.py
index 63a8417..18e58a7 100644
--- a/agent_system/environments/env_package/webshop/projection.py
+++ b/agent_system/environments/env_package/webshop/projection.py
@@ -16,6 +16,9 @@
 from typing import List
 import re
 
+# Lightweight observability (webshop manager doesn't report valid_action_ratio).
+_WS_DIAG = {"n": 0, "valid": 0, "cn": 0, "last": 0}
+
 def webshop_projection(actions: List[str]):
     """
     A function to process the actions.
@@ -25,9 +28,12 @@ def webshop_projection(actions: List[str]):
     """
 
     valids = [0] * len(actions)
+    _cn = 0  # diagnostic: count of Chinese-containing generations this call
 
     for i in range(len(actions)):
         original_str = actions[i]  # keep the original string
+        if re.search(r'[一-鿿]', original_str):
+            _cn += 1
         actions[i] = actions[i].lower()
 
         # Attempt to extract the substring within <action>...</action>
@@ -61,4 +67,18 @@ def webshop_projection(actions: List[str]):
         if re.search(r'[\u4e00-\u9fff]', original_str):
             valids[i] = 0
 
+    # Lightweight observability (webshop manager doesn't report valid_action_ratio).
+    # Diagnostic only; no effect on returned values.
+    try:
+        _WS_DIAG["n"] += len(actions)
+        _WS_DIAG["valid"] += sum(valids)
+        _WS_DIAG["cn"] += _cn
+        if _WS_DIAG["n"] - _WS_DIAG.get("last", 0) >= 128:
+            _WS_DIAG["last"] = _WS_DIAG["n"]
+            print(f"[WS_PROJ_DIAG] n={_WS_DIAG['n']} valid_rate="
+                  f"{_WS_DIAG['valid']/max(_WS_DIAG['n'],1):.3f} "
+                  f"chinese_rate={_WS_DIAG['cn']/max(_WS_DIAG['n'],1):.3f}", flush=True)
+    except Exception:
+        pass
+
     return actions, valids
\ No newline at end of file
diff --git a/agent_system/environments/prompts/alfworld.py b/agent_system/environments/prompts/alfworld.py
index 6f72445..0aa7f95 100644
--- a/agent_system/environments/prompts/alfworld.py
+++ b/agent_system/environments/prompts/alfworld.py
@@ -14,13 +14,35 @@
 # limitations under the License.
 
 # --------------------- ALFWorld --------------------- #
+# General behavioral guidance, identical across the two templates below. Keeps
+# the prompt body aligned across training and the envharness pure_eval pipeline
+# (examples/run_alfworld_pure_eval.py SYSTEM_PROMPT carries the same bullets).
+# These bullets are non-task-specific; they nudge against pathological loops
+# the model exhibited at eval time (Qwen3-8B non-thinking OOD jumped from 20%
+# to 30% after these were added to the policy's system message).
+_ALFWORLD_TIPS = (
+    "Tips for completing the task efficiently:\n"
+    "- Commit to a plan that satisfies the goal; re-read it each turn.\n"
+    "- Do NOT repeat the same command twice in a row. If an observation says\n"
+    "  \"Nothing happens\" or repeats the previous one, your last command\n"
+    "  was ineffective -- pick a DIFFERENT admissible command, not the same\n"
+    "  one again.\n"
+    "- \"look\" and \"inventory\" are diagnostic; use them sparingly. Make\n"
+    "  progress by navigating (go to ...), opening containers, taking items,\n"
+    "  and using destinations (sinkbasin / microwave / fridge / lamp) when\n"
+    "  the goal needs them.\n"
+    "- You have a budget of 50 steps; be decisive."
+)
+
 ALFWORLD_TEMPLATE_NO_HIS = """
 You are an expert agent operating in the ALFRED Embodied Environment.
 Your current observation is: {current_observation}
 Your admissible actions of the current situation are: [{admissible_actions}].
 
+""" + _ALFWORLD_TIPS + """
+
 Now it's your turn to take an action.
-You should first reason step-by-step about the current situation. This reasoning process MUST be enclosed within <think> </think> tags. 
+You should first reason step-by-step about the current situation. This reasoning process MUST be enclosed within <think> </think> tags.
 Once you've finished your reasoning, you should choose an admissible action for current step and present it within <action> </action> tags.
 """
 
@@ -30,7 +52,9 @@ Prior to this step, you have already taken {step_count} step(s). Below are the m
 You are now at step {current_step} and your current observation is: {current_observation}
 Your admissible actions of the current situation are: [{admissible_actions}].
 
+""" + _ALFWORLD_TIPS + """
+
 Now it's your turn to take an action.
-You should first reason step-by-step about the current situation. This reasoning process MUST be enclosed within <think> </think> tags. 
+You should first reason step-by-step about the current situation. This reasoning process MUST be enclosed within <think> </think> tags.
 Once you've finished your reasoning, you should choose an admissible action for current step and present it within <action> </action> tags.
 """
\ No newline at end of file
diff --git a/agent_system/multi_turn_rollout/rollout_loop.py b/agent_system/multi_turn_rollout/rollout_loop.py
index d79e678..14d3dd3 100644
--- a/agent_system/multi_turn_rollout/rollout_loop.py
+++ b/agent_system/multi_turn_rollout/rollout_loop.py
@@ -62,6 +62,20 @@ class TrajectoryCollector:
         raw_prompt = gen_batch.non_tensor_batch['raw_prompt'][item]
         data_source = gen_batch.non_tensor_batch['data_source'][item]
         apply_chat_template_kwargs = self.config.data.get("apply_chat_template_kwargs", {})
+        # ENVHARNESS PATCH: opt-in non-thinking-mode chat template for Qwen3.
+        # Hydra CLI can't parse nested-dict JSON values easily, so we honor an
+        # env-var instead: setting ENVHARNESS_DISABLE_THINKING=1 forces
+        # chat_template_kwargs={"enable_thinking": False}, which makes
+        # Qwen3's chat template inject `<think>\n\n</think>` (empty) before
+        # the model generates -- aligning training rollouts with non-thinking
+        # eval. Models without that flag in their template silently ignore it.
+        import os as _os
+        if _os.environ.get("ENVHARNESS_DISABLE_THINKING", "").lower() in ("1", "true", "yes"):
+            ack = dict(apply_chat_template_kwargs)
+            ack.setdefault("chat_template_kwargs", {})
+            ack["chat_template_kwargs"] = dict(ack["chat_template_kwargs"])
+            ack["chat_template_kwargs"]["enable_thinking"] = False
+            apply_chat_template_kwargs = ack
         
         # Get observation components
         obs_texts = obs.get('text', None)
@@ -79,19 +93,22 @@ class TrajectoryCollector:
         # if '<image>' in obs_content: 
         #     obs_content = obs_content.replace('<image>', '')
 
-        # Build chat structure
-        obs_content = ''
-        if obs_text is not None:
-            obs_content += obs_text
+        # Build chat structure. CHAT path: env_manager returns a proper multi-turn
+        # message list (list of {role,content} dicts) so training is in-distribution
+        # for the chat-SFT'd model and consistent with the eval. Legacy flattened path:
+        # obs_text is a single string wrapped as one user message.
+        if isinstance(obs_text, (list, tuple)):
+            chat = list(obs_text)   # plain list of {role,content} dicts (NOT np.array:
+                                    # apply_chat_template does ambiguous truthiness on arrays)
         else:
-            print(f"Warning: No text observation found!")
+            obs_content = obs_text if obs_text is not None else ''
+            if obs_text is None:
+                print("Warning: No text observation found!")
+            chat = np.array([{
+                "content": obs_content,
+                "role": "user",
+            }])
 
-        
-        chat = np.array([{
-            "content": obs_content,
-            "role": "user",
-        }])
-        
         # Apply chat template
         prompt_with_chat_template = self.tokenizer.apply_chat_template(
             chat,
@@ -183,7 +200,7 @@ class TrajectoryCollector:
         })
 
         if self.config.data.get('return_raw_chat', False):
-            row_dict['raw_prompt'] = chat.tolist()
+            row_dict['raw_prompt'] = chat.tolist() if hasattr(chat, 'tolist') else list(chat)
         
         return row_dict
 
diff --git a/verl/trainer/constants_ppo.py b/verl/trainer/constants_ppo.py
index 7c3a0bf..2c531de 100644
--- a/verl/trainer/constants_ppo.py
+++ b/verl/trainer/constants_ppo.py
@@ -31,6 +31,12 @@ PPO_RAY_RUNTIME_ENV = {
         # https://docs.vllm.ai/en/latest/usage/troubleshooting.html?h=nccl_cumem_enable#known-issues
         # https://github.com/vllm-project/vllm/blob/c6b0a7d3ba03ca414be1174e9bd86a97191b7090/vllm/worker/worker_base.py#L445
         "NCCL_CUMEM_ENABLE": "0",
+        # vLLM 0.17 v1 engine hangs on CPU Gloo all_reduce during KV-cache
+        # init when launched under Ray pool (verl-agent's setup). Forcing
+        # the v0 engine works around this until vllm/verl-agent compat is
+        # fixed. Must be in Ray runtime_env so it propagates to workers
+        # (a plain shell `export VLLM_USE_V1=0` does NOT reach Ray workers).
+        "VLLM_USE_V1": "0",
     },
 }
 
diff --git a/verl/trainer/ppo/ray_trainer.py b/verl/trainer/ppo/ray_trainer.py
index d85e23a..01a11ba 100644
--- a/verl/trainer/ppo/ray_trainer.py
+++ b/verl/trainer/ppo/ray_trainer.py
@@ -1044,6 +1044,17 @@ class RayPPOTrainer:
         self.global_steps += 1
         last_val_metrics = None
 
+        # ---- DAPO dynamic sampling (filter_groups) accumulator state ----
+        # Ported from recipe/dapo/dapo_ray_trainer.py into the AGENTIC loop:
+        # drop groups whose GROUP_N rollouts all got the same reward (std==0, no
+        # gradient signal) and keep generating fresh batches until >= train_batch_size
+        # informative groups are collected (or max_num_gen_batches is hit).
+        _fg = getattr(self.config.algorithm, "filter_groups", None)
+        _ds_enable = bool(getattr(_fg, "enable", False)) if _fg is not None else False
+        _ds_batch = None
+        _ds_num_prompt = 0
+        _ds_num_gen = 0
+
         for epoch in range(self.config.trainer.total_epochs):
             for batch_dict in self.train_dataloader:
                 metrics = {}
@@ -1138,6 +1149,48 @@ class RayPPOTrainer:
                         else:
                             reward_tensor, reward_extra_infos_dict = compute_reward(batch, self.reward_fn)
 
+                    # ---- DAPO dynamic sampling: drop zero-variance groups + accumulate ----
+                    # Runs BEFORE the expensive old_log_prob/ref/adv/update so filtered
+                    # generations cost only rollout+reward. Groups (GROUP_N rollouts of one
+                    # task, keyed by uid) whose rewards are all equal (std==0) give no GRPO
+                    # gradient and are dropped; we keep generating fresh batches until we have
+                    # >= train_batch_size informative groups or hit max_num_gen_batches.
+                    if _ds_enable and not self.config.reward_model.launch_reward_fn_async:
+                        batch.batch["token_level_scores"] = reward_tensor
+                        if reward_extra_infos_dict:
+                            batch.non_tensor_batch.update({k: np.array(v) for k, v in reward_extra_infos_dict.items()})
+                        _seq_r = reward_tensor.sum(dim=-1).numpy()
+                        _uids = batch.non_tensor_batch["uid"]
+                        _u2v = defaultdict(list)
+                        for _u, _r in zip(_uids, _seq_r):
+                            _u2v[_u].append(_r)
+                        _kept = {u for u, v in _u2v.items() if np.std(v) > 0 or len(v) == 1}
+                        _keep_idx = [i for i, u in enumerate(_uids) if u in _kept]
+                        _ds_num_gen += 1
+                        if _keep_idx:
+                            _fb = batch[_keep_idx]
+                            _ds_batch = _fb if _ds_batch is None else DataProto.concat([_ds_batch, _fb])
+                            _ds_num_prompt += len(_kept)
+                        _prompt_bsz = self.config.data.train_batch_size
+                        _max_gen = self.config.algorithm.filter_groups.max_num_gen_batches
+                        print(f"[dynamic-sampling] gen_batch={_ds_num_gen} informative_groups={_ds_num_prompt}/{_prompt_bsz}")
+                        if _ds_num_prompt < _prompt_bsz and (_max_gen <= 0 or _ds_num_gen < _max_gen):
+                            continue  # not enough signal yet -> generate another batch
+                        _traj_bsz = self.config.data.train_batch_size * self.config.actor_rollout_ref.rollout.n
+                        if _ds_batch is not None:
+                            batch = _ds_batch[:_traj_bsz]
+                            reward_tensor = batch.batch["token_level_scores"]
+                        else:
+                            # No informative groups after max_num_gen_batches (all rollouts in
+                            # every group got the same reward). Fall back to the last unfiltered
+                            # batch so training proceeds -- its GRPO advantage is ~0 (a harmless
+                            # no-op step) instead of crashing. token_level_scores already set above.
+                            print("[dynamic-sampling] no informative groups this step; using last unfiltered batch")
+                        reward_extra_infos_dict = {}          # already inside batch.non_tensor_batch
+                        batch.batch["response_mask"] = compute_response_mask(batch)
+                        batch.meta_info["global_token_num"] = torch.sum(batch.batch["attention_mask"], dim=-1).tolist()
+                        _ds_batch = None; _ds_num_prompt = 0; _ds_num_gen = 0
+
                     # recompute old_log_probs
                     with _timer("old_log_prob", timing_raw):
                         old_log_prob = self.actor_rollout_wg.compute_log_prob(batch)
diff --git a/verl/utils/checkpoint/fsdp_checkpoint_manager.py b/verl/utils/checkpoint/fsdp_checkpoint_manager.py
index f598012..c0937dc 100644
--- a/verl/utils/checkpoint/fsdp_checkpoint_manager.py
+++ b/verl/utils/checkpoint/fsdp_checkpoint_manager.py
@@ -225,7 +225,10 @@ class FSDPCheckpointManager(BaseCheckpointManager):
 
                     auto_model_cls = AutoModelForCausalLM
                 elif "ForConditionalGeneration" in model_config.architectures[0]:
-                    from transformers import AutoModelForVision2Seq
+                    try:
+                        from transformers import AutoModelForVision2Seq
+                    except ImportError:
+                        from transformers import AutoModelForImageTextToText as AutoModelForVision2Seq
 
                     auto_model_cls = AutoModelForVision2Seq
                 else:
diff --git a/verl/utils/vllm_utils.py b/verl/utils/vllm_utils.py
index 3778877..fec182a 100644
--- a/verl/utils/vllm_utils.py
+++ b/verl/utils/vllm_utils.py
@@ -69,7 +69,12 @@ from typing import List
 
 from msgspec import field
 from packaging import version as vs
-from vllm.lora.models import LoRAModel
+# vLLM moved LoRAModel from `vllm.lora.models` -> `vllm.lora.lora_model`
+# around v0.10. Support both layouts so the verl-agent fork runs on either.
+try:
+    from vllm.lora.models import LoRAModel
+except ImportError:
+    from vllm.lora.lora_model import LoRAModel  # vllm >= 0.10
 from vllm.lora.request import LoRARequest
 from vllm.lora.utils import get_adapter_absolute_path
 from vllm.lora.worker_manager import LRUCacheWorkerLoRAManager
diff --git a/verl/workers/fsdp_workers.py b/verl/workers/fsdp_workers.py
index 7d9b8b4..ba36f1b 100644
--- a/verl/workers/fsdp_workers.py
+++ b/verl/workers/fsdp_workers.py
@@ -185,7 +185,14 @@ class ActorRolloutRefWorker(Worker):
         from torch import optim
         from torch.distributed.fsdp import CPUOffload, MixedPrecision
         from torch.distributed.fsdp import FullyShardedDataParallel as FSDP
-        from transformers import AutoConfig, AutoModelForCausalLM, AutoModelForVision2Seq
+        from transformers import AutoConfig, AutoModelForCausalLM
+
+        try:
+            from transformers import AutoModelForVision2Seq
+        except ImportError:
+            # transformers 5.x renamed this to AutoModelForImageTextToText
+            # (needed for qwen3_5 / Qwen3.5). Alias keeps tf4 + tf5 both working.
+            from transformers import AutoModelForImageTextToText as AutoModelForVision2Seq
 
         from verl.utils.model import get_generation_config, print_model_size, update_model_config
         from verl.utils.torch_dtypes import PrecisionType
diff --git a/verl/workers/sharding_manager/fsdp_vllm.py b/verl/workers/sharding_manager/fsdp_vllm.py
index d5249ce..9ea19de 100644
--- a/verl/workers/sharding_manager/fsdp_vllm.py
+++ b/verl/workers/sharding_manager/fsdp_vllm.py
@@ -246,7 +246,13 @@ class FSDPVLLMShardingManager(BaseShardingManager):
         ):
             group = vllm_ps.get_tensor_model_parallel_group()
         else:
-            group = vllm_ps.get_tensor_model_parallel_group().device_group
+            # vLLM >=0.7 dropped `get_tensor_model_parallel_group`; the
+            # equivalent is `get_tp_group()` which returns the same
+            # GroupCoordinator (carries `.device_group`).
+            if hasattr(vllm_ps, "get_tensor_model_parallel_group"):
+                group = vllm_ps.get_tensor_model_parallel_group().device_group
+            else:
+                group = vllm_ps.get_tp_group().device_group
 
         all_gather_data_proto(data=data, process_group=group)
         return data
